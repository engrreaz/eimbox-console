# EIMBox - Payment Dues Synchronization Engine (`sync-payments.php`)

---

## ১. পরিচিতি ও মূল উদ্দেশ্য (Overview & Purpose)

`sync-payments.php` হলো EIMBox শিক্ষাপ্রতিষ্ঠান ম্যানেজমেন্ট সিস্টেমের একটি অত্যন্ত সংবেদনশীল ও শক্তিশালী **ব্যাচ পেমেন্ট সিঙ্ক্রোনাইজেশন ইঞ্জিন (Batch Dues Sync Engine)**।

### প্রধান উদ্দেশ্য:
1. **ফি পলিসি শিক্ষার্থীদের অ্যাকাউন্টে প্রয়োগ করা:** প্রতিষ্ঠান কর্তৃক `payment-settings.php` এ নির্ধারিত মাসিক বেতন, ভর্তি ফি, সেশন ফি, পরীক্ষার ফি ইত্যাদি ফি আইটেম ও শ্রেণিভিত্তিক রেট শিক্ষার্থীদের ব্যক্তিগত লেজার টেবিল `stfinance`-এ স্বয়ংক্রিয়ভাবে জেনারেট বা আপডেট করা।
2. **বকেয়া (Dues) হিসাব পুনরায় নির্ভুল করা:** কোনো শ্রেণির বা আইটেমের ফি পরিবর্তন করা হলে শিক্ষার্থীভেদে বকেয়া নির্ভুলভাবে পুনর্গণনা করা।
3. **আর্থিক তথ্যের পূর্ণ নিরাপত্তা ও অপরিবর্তনশীলতা (Zero Data Loss Guarantee):** যেসকল শিক্ষার্থীর ফি বাবদ ইতোমধ্যে আংশিক বা পূর্ণ টাকা আদায় হয়েছে অথবা মানি রসিদ (`stpr`) তৈরি হয়েছে, তাদের পূর্বের পরিশোধিত হিসাব বা আদায়কৃত অর্থ কোনোভাবেই ক্ষতিগ্রস্ত, মুছে ফেলা বা পরিবর্তন না করা।

---

## ২. ইউজার ইন্টারফেস ও কম্পোনেন্টসমূহ (UI Components)

### ক. Slot / Session Filter Bar
- `components/slot-tree-ui.php` এর মাধ্যমে ব্রাঞ্চ/শিফট (`slot`) এবং শিক্ষাবর্ষ (`session`) নির্বাচন করা হয়।
- কুকি (`slot`, `chain-slot`, `session`, `chain-session`) অথবা `$_GET` প্যারামিটারের মাধ্যমে স্টেট ধরে রাখা হয়।

### খ. অ্যাডভান্সড সিক্স-প্যারামিটার ফিল্টারিং (`payments/six-parameters.php`)
প্রয়োজন অনুযায়ী নির্দিষ্ট ক্রাইটেরিয়ায় ফিল্টার করে সিঙ্ক করার সুবিধা:
1. **Filter Type (`type`):** `overall` (সকল শিক্ষার্থী), `item` (নির্দিষ্ট আইটেম), `student` (নির্দিষ্ট শিক্ষার্থী), `class` (নির্দিষ্ট শ্রেণি), `section` (নির্দিষ্ট শাখা)।
2. **Range (`part`):** `all` (সকল আইটেম) অথবা `ind` (ব্যক্তিগত কনসেশন সেটআপ)।
3. **Fee Item (`icode`):** `financesetup` থেকে নির্দিষ্ট কোনো ফি আইটেম সিলেক্ট করা।
4. **Student ID (`stid`):** নির্দিষ্ট শিক্ষার্থীর আইডি লিখে শুধু তার হিসাব সিঙ্ক করা।
5. **Class (`cls`):** নির্দিষ্ট শ্রেণি নির্বাচন।
6. **Section (`sec`):** নির্বাচিত শ্রেণির অধীনস্থ শাখা নির্বাচন (AJAX এর মাধ্যমে স্বয়ংক্রিয়ভাবে লোড হয়)।

### গ. লাইভ স্ট্যাটাস KPI ও প্রগ্রেস বার (Status KPI & Realtime Progress)
- **Pending Students (`#countus`):** যেসকল শিক্ষার্থীর ভ্যালিডেশন বা সিঙ্ক বাকি আছে (`sessioninfo.validate = 0`)।
- **Total Filtered:** ফিল্টারের আওতাভুক্ত মোট শিক্ষার্থী সংখ্যা।
- **Estimated Remaining Time (`#remain`):** আনুমানিক অবশিষ্ট সেকেন্ড (প্রতি সেকেন্ডে ২৫ জন শিক্ষার্থী হিসেবে `ceil(left / 25)` সেকেন্ড)।
- **Progress Bar (`#prog`):** লাইভ অ্যানিমেটেড পার্সেন্টেজ বার (`0%` থেকে `100%`)।

### ঘ. অ্যাকশন বাটনসমূহ
- **Sync Now (`#syncBtn`):** কিউতে থাকা অনিষ্পন্ন শিক্ষার্থীদের ব্যাচ প্রসেসিং শুরু করে।
- **Reset Validation Queue (`resetAll()`):** `sessioninfo.validate = 0` করে দেয় যাতে সকল শিক্ষার্থীর হিসাব নতুন করে প্রথম থেকে সিঙ্ক করা যায়।

### ঙ. রিয়েলটাইম প্রসেস লগ (`#log`)
- টার্মিনাল স্টাইলের লাইভ লগ উইন্ডো, যেখানে প্রতি ব্যাচে কতজন নতুন আইটেম যুক্ত হলো (`+new`), কতটি বকেয়া আপডেট হলো (`~upd`), এবং কতটি পেইড রেকর্ড লক করা রইলো (`locked`) তা টাইমস্ট্যাম্পসহ প্রদর্শিত হয়।

---

## ৩. কিউই ম্যানেজমেন্ট ও ভ্যালিডেশন লাইফসাইকেল (Queue Management Lifecycle)

সিস্টেমটি **Validation Flag (`validate`)** ভিত্তিক কিউই আর্কিটেকচারে কাজ করে:

```
[ ইউজার "Reset Validation Queue" চাপলে ]
               │
               ▼
  sessioninfo.validate = 0 (সকল শিক্ষার্থী কিউতে যুক্ত হয়)
               │
               ▼
[ ইউজার "Sync Now" চাপলে বা স্বয়ংক্রিয় রিকল হলে ]
               │
               ▼
  SELECT ... FROM sessioninfo WHERE validate = 0 LIMIT 25
               │
               ▼
       [ ব্যাচ প্রসেসিং সম্পন্ন ]
               │
               ▼
  sessioninfo.validate = 1 (ওই ২৫ জন সম্পন্ন হিসেবে চিহ্নিত হয়)
               │
               ▼
  অবশিষ্ট পেন্ডিং কাউন্ট > 0 হলে পুনরায় পরবর্তী ২৫ জনকে প্রসেস করে
               │
               ▼
  পেন্ডিং কাউন্ট == 0 হলে "Sync Complete"
```

---

## ৪. ব্যাকএন্ড ইঞ্জিন (`payments/check-student-finance.php`) এর গভীর বিশ্লেষণ

`sync-payments.php` পেজের মূল ব্যাকএন্ড প্রসেসর হলো `payments/check-student-finance.php`।

### ক. ব্যাচ প্রসেসিং ও চাঙ্ক সাইজ (High-Speed Batching)
- সার্ভার মেমোরি ও টাইমআউট এড়াতে প্রতি AJAX রিকোয়েস্টে **২৫ জন শিক্ষার্থী** (`$batchSize = 25`) প্রসেস হয়।
- নির্দিষ্ট একজন শিক্ষার্থীর ক্ষেত্রে চাঙ্ক সাইজ **১ জন**।

### খ. অ্যাটোমিক ট্রানজ্যাকশন (ACID Atomic Transaction)
- ডেটাবেসে আংশিক ডেটা লেখা রোধ করতে `$conn->begin_transaction()` ব্যবহার করা হয়।
- ব্যাচের মাঝে কোনো ত্রুটি ঘটলে `$conn->rollback()` হয়ে পূর্বাবস্থায় ফিরে যায় এবং স্পষ্ট ত্রুটির বার্তা পাঠায়। সফল হলে `$conn->commit()` হয়।

### গ. ইন-মেমোরি প্রি-ফেচিং (In-Memory Pre-fetching - Zero N+1 Queries)
লুপের ভেতর বারবার ডেটাবেস কোয়েরি না চালিয়ে, ব্যাচ শুরুর পূর্বেই প্রয়োজনীয় সমস্ত ডেটা মেমোরি হ্যাশ ম্যাপে লোড করে নেওয়া হয়:
1. **`$indMap` (Individual Setup):** `financesetupind` টেবিল থেকে ওই ২৫ জন শিক্ষার্থীর বিশেষ ছাড়/কনসেশন রেট।
2. **`$finSetup` (Master Fee Items):** `financesetup` টেবিল থেকে সংশ্লিষ্ট সেশনের সক্রিয় ফি আইটেমসমূহ (`active = 1`)।
3. **`$valMap` (Class-wise Amounts):** `financesetupvalue` টেবিল থেকে `itemcode|classname|sectionname` ভিত্তিক টাকার পরিমাণ।
4. **`$stFinMap` (Existing Ledgers):** `stfinance` টেবিল থেকে ওই শিক্ষার্থীদের বিদ্যমান সকল লেজার এন্ট্রি।

### ঘ. মাস ও ফ্রিকোয়েন্সি রেজোলিউশন (`parseRuleMonths()`)
ফি আইটেমের ফ্রিকোয়েন্সি কোড অনুসারে প্রযোজ্য মাস নির্ধারণ:
- `0` ➔ প্রতি মাসে (মাস ১ থেকে ১২: `range(1, 12)`)
- `1` থেকে `12` ➔ নির্দিষ্ট একক মাস (যেমন: `1` = জানুয়ারি, `12` = ডিসেম্বর)
- `22` ➔ ২ মাস অন্তর (ফেব্রুয়ারি, এপ্রিল, জুন, আগস্ট, অক্টোবর, ডিসেম্বর)
- `33` ➔ ৩ মাস অন্তর / ত্রৈমাসিক (মার্চ, জুন, সেপ্টেম্বর, নভেম্বর)
- `44` বা `442` ➔ ৪ মাস অন্তর (এপ্রিল, আগস্ট, নভেম্বর)
- `66` বা `662` ➔ ৬ মাস অন্তর / ষান্মাসিক (জানুয়ারি, নভেম্বর)
- `67` ➔ জুন ও নভেম্বর

### ঙ. ফি নির্ধারণের অগ্রাধিকার ও শর্তসমূহ (Fee Calculation Priority):
1. **New Admission Only ফিল্টার:** আইটেমে `new_only = 1` থাকলে এবং শিক্ষার্থী নতুন ভর্তি না হলে (`sessioninfo.new_admi == 0`) আইটেমটি বাদ দেওয়া হয়।
2. **টাকার পরিমাণ নির্ধারণের ক্রম (Fallback Hierarchy):**
   - **১ম অগ্রাধিকার:** শ্রেণি + শাখা স্পেসিফিক রেট (`itemcode|classname|sectionname`)
   - **২য় অগ্রাধিকার:** শ্রেণি স্পেসিফিক রেট (`itemcode|classname|`)
   - **৩য় অগ্রাধিকার:** গ্লোবাল ডিফল্ট রেট (`itemcode||`)
3. **বেতন / টিউশন ফি ওয়েভার (Tuition Fee Waiver):**
   - আইটেমের নামে `tuition`, `tution` অথবা বাংলায় `বেতন` থাকলে শিক্ষার্থীর নির্ধারিত ওয়েভার পার্সেন্টেজ প্রয়োগ করা হয়:
     $$\text{Amount} = \frac{\text{Base Amount} \times \text{sessioninfo.rate}}{100}$$
4. **ব্যক্তিগত বিশেষ ছাড় (Individual Override):**
   - `financesetupind` এ শিক্ষার্থীর জন্য আলাদা কোনো নির্ধারিত রেট থাকলে তা চূড়ান্ত `payableamt` হিসেবে ধার্য হয়।

---

## ৫. আর্থিক তথ্যের নিরাপত্তা নিশ্চিতকরণ (Triple-Guarded Financial Safety)

সিস্টেমে টাকা আদায়ের তথ্য সম্পূর্ণ সুরক্ষিত রাখার জন্য ৩ স্তরের নিরাপত্তা ব্যবস্থা রয়েছে:

### স্তর ১: পেইড রেকর্ড প্রটেকশন (Paid Records Lock)
যদি `stfinance` এ বিদ্যমান কোনো রেকর্ডে টাকা জমা থাকে:
- `paid > 0` অথবা
- `pr1 > 0` অথবা
- `pr1no > 0` (মানি রসিদ নম্বর বিদ্যমান) অথবা
- `pr2 > 0` অথবা
- `splitid > 0`

➔ **এই রেকর্ডগুলোর টাকা বা বকেয়া স্পর্শ করা হয় না (`validateOnlyIds`)। শুধুমাত্র `validate = 1` ও চার্ট অফ অ্যাকাউন্টস সাব-হেড আপডেট করা হয়।**

### স্তর ২: অবৈতনিক বকেয়া রিক্যালকুলেশন (Unpaid Dues Recalculation)
যেসব রেকর্ডে কোনো টাকা জমা পড়েনি (`paid = 0` এবং `pr1no = 0`), ফি পরিবর্তনের কারণে সেগুলোর `amount`, `payableamt`, এবং `dues` নতুন রেট অনুযায়ী আপডেট করা হয়।

### স্তর ৩: অপসারিত আইটেমের নিরাপদ ক্লিনআপ (Safe Deletion of Removed Unpaid Items)
যদি কোনো ফি আইটেম প্রতিষ্ঠান বাতিল করে দেয়, তবে কেবল সেই রেকর্ডগুলোই মুছে ফেলা হয় যা:
1. সংশ্লিষ্ট ব্যাচে `validate = 0` রয়েছে।
2. এবং যার বিপরীতে **কোনো টাকা পরিশোধ হয়নি** (`paid = 0`, `pr1 = 0`, `pr1no IS NULL`, `pr2 = 0`, `cashbook1 = 0`, `cashbook2 = 0`)।

---

## ৬. ক্লায়েন্ট-সাইড জাভাস্ক্রিপ্ট ও রিকার্সিভ AJAX ফ্লো

```javascript
function processSync() {
    $.post('payments/check-student-finance.php', filterData, function (res) {
        // ১. প্রসেস লগ প্রি-পেন্ড
        $('#log').prepend(res);

        // ২. অবশিষ্ট কিউ সংখ্যা রিড
        let left = parseInt($('#totaltotal').text());
        $('#countus').text(left);

        // ৩. প্রগ্রেস বার ও অবশিষ্ট সময় আপডেট
        let completed = initialTotal - left;
        let p = Math.round((completed / initialTotal) * 100);
        $('#prog').css('width', p + '%').text(p + '%');

        // ৪. রিকার্সিভ কল: যদি এখনো শিক্ষার্থী বাকি থাকে, তবে পরবর্তী ব্যাচ চালাও
        if (left > 0) {
            processSync();
        } else {
            // ৫. সম্পন্ন হলে ব্যাজ ও বাটন কমপ্লিট স্টেটে নিয়ে যাও
            $('#prog').addClass('bg-success');
            $('#syncBtn').html('<i class="bi bi-check-circle me-1"></i> Sync Complete');
        }
    });
}
```

---

## ৭. সংশ্লিষ্ট ডেটাবেস টেবিল ও ফিল্ড ম্যাপিং

| টেবিলের নাম | ব্যবহারের উদ্দেশ্য | মূল কলামসমূহ |
| :--- | :--- | :--- |
| `sessioninfo` | শিক্ষার্থীর সেশনভিত্তিক তালিকা ও কিউই স্টেট | `stid`, `classname`, `sectionname`, `rollno`, `rate`, `sessionyear`, `validate`, `validationtime` |
| `financesetup` | প্রতিষ্ঠানের মাস্টার ফি আইটেম ও ফ্রিকোয়েন্সি তালিকা | `id`, `itemcode`, `month`, `particulareng`, `particularben`, `new_only`, `splitable`, `sub_head`, `active` |
| `financesetupvalue` | শ্রেণি ও শাখাভিত্তিক ফি রেট | `itemcode`, `classname`, `sectionname`, `amount`, `sessionyear`, `slot` |
| `financesetupind` | শিক্ষার্থীর ব্যক্তিগত বিশেষ ফি বা কনসেশন রেট | `stid`, `itemcode`, `amount`, `sessionyear` |
| `stfinance` | শিক্ষার্থীর একক লেজার রেকর্ড (মাসিক হিসাব) | `stid`, `itemcode`, `month`, `amount`, `payableamt`, `paid`, `dues`, `pr1`, `pr1no`, `pr1date`, `validate` |
| `stpr` | ফি আদায়ের মানি রসিদ মাস্টার রেকর্ড | `stid`, `prno`, `prdate`, `amount`, `sessionyear` |
| `account_sub_head` | চার্ট অফ অ্যাকাউন্টস সাব-হেড ম্যাপিং | `id`, `account_head_id`, `sub_head`, `income` |

---

## ৮. সংক্ষেপে মূল বৈশিষ্ট্যসমূহ

- ⚡ **হাই-স্পিড ব্যাচিং:** ২৫ জন করে শিক্ষার্থী প্রসেস হওয়ায় ব্রাউজার হ্যাং বা মেমোরি লিমিট এক্সিড হওয়ার ঝুঁকি নেই।
- 🛡️ **ট্রিপল-গার্ডেড নিরাপত্তা:** আদায়কৃত একটি টাকার হিসাবও নষ্ট হওয়া অসম্ভব।
- 🔄 **স্বয়ংক্রিয় রিকার্শন:** ক্লিক করার পর স্বয়ংক্রিয়ভাবে পুরো প্রতিষ্ঠান/শ্রেণির সিঙ্ক শেষ করে নোটিফিকেশন প্রদান করে।
- 🎯 **নির্ভুল ওয়েভার ও কনসেশন:** সাধারণ শিক্ষার্থীদের পাশাপাশি ওয়েভারপ্রাপ্ত ও ব্যক্তিগত ছাড়প্রাপ্ত শিক্ষার্থীদের হিসাব নির্ভুলভাবে সংরক্ষিত থাকে।
