# পরিকল্পনা: শিক্ষক পারফরম্যান্স অ্যানালাইসিস মডিউল

এই পরিকল্পনাটি `eimbox` ড্যাশবোর্ডে শিক্ষার্থীদের পরীক্ষার ফলাফলের উপর ভিত্তি করে শিক্ষকদের পারফরম্যান্স বিশ্লেষণের ফিচার যুক্ত করার জন্য তৈরি করা হয়েছে।

## ধাপ ১: ডাটাবেস প্রস্তুতি এবং যাচাই

- [x] `eimbox_analytics` নামে একটি নতুন ডাটাবেস তৈরি করা হয়েছে। (`install.sql`)
- [x] `analytics_teacher_performance` টেবিলটি প্রয়োজনীয় কলাম (pass_rate, average_gpa, a_plus_count ইত্যাদি) সহ তৈরি করা হয়েছে। (`install.sql`)
- [x] `install.sql` স্ক্রিপ্টটি চালিয়ে ডাটাবেস এবং টেবিলের গঠন চূড়ান্তভাবে যাচাই করা।

## ধাপ ২: ব্যাকএন্ড - ডেটা প্রসেসিং ইঞ্জিন তৈরি

- [x] একটি কোর PHP স্ক্রিপ্ট তৈরি করা (যেমন: `jobs/process_teacher_performance.php`) যা অ্যানালাইসিসের মূল কাজটি করবে।
- [x] **শিক্ষক-বিষয়-শ্রেণি ম্যাপিং:** প্রধান ডাটাবেসের `subsetup` টেবিল থেকে তথ্য নিয়ে কোন শিক্ষক কোন ক্লাসে কোন বিষয় পড়ান তা বের করার লজিক লেখা।
- [x] **ফলাফল সংগ্রহ:** প্রতিটি শিক্ষক-বিষয়-শ্রেণি কম্বিনেশনের জন্য `stmark` টেবিল থেকে সংশ্লিষ্ট সকল শিক্ষার্থীর নম্বর ও জিপিএ সংগ্রহ করার কোয়েরি লেখা।
- [x] **পারফরম্যান্স মেট্রিক গণনা:** সংগৃহীত ডেটার উপর ভিত্তি করে `analytics_teacher_performance` টেবিলের কলাম অনুযায়ী মানগুলো (গড় জিপিএ, পাসের হার ইত্যাদি) গণনা করার ফাংশন তৈরি করা।
- [x] **ডেটা সংরক্ষণ:** গণনা করা মেট্রিকগুলো `eimbox_analytics.analytics_teacher_performance` টেবিলে `INSERT` বা `UPDATE` করার জন্য `ON DUPLICATE KEY UPDATE` স্টেটমেন্ট ব্যবহার করা।

## ধাপ ২.৫: পারফরম্যান্স মেট্রিক গণনার সূত্রাবলী

প্রতিটি শিক্ষক-বিষয়-শ্রেণি কম্বিনেশনের জন্য নিচের সূত্রগুলো ব্যবহার করে পারফরম্যান্স গণনা করা হবে:

- **`total_students`**: `COUNT(DISTINCT stmark.stid)` - ঐ বিষয়ে মোট পরীক্ষার্থীর সংখ্যা।
- **`total_pass`**: `SUM(CASE WHEN stmark.gpa > 0 THEN 1 ELSE 0 END)` - মোট পাস করা শিক্ষার্থীর সংখ্যা।
- **`total_fail`**: `SUM(CASE WHEN stmark.gpa = 0 THEN 1 ELSE 0 END)` - মোট ফেল করা শিক্ষার্থীর সংখ্যা।
- **`pass_rate`**: `(total_pass / total_students) * 100` - পাসের হার (শতাংশ)।
- **`average_gpa`**: `AVG(stmark.gpa)` - শিক্ষার্থীদের গড় জিপিএ।
- **`average_marks`**: `AVG(stmark.on100)` - শিক্ষার্থীদের গড় নম্বর (১০০ এর মধ্যে)।
- **`highest_gpa`**: `MAX(stmark.gpa)` - ঐ বিষয়ে সর্বোচ্চ প্রাপ্ত জিপিএ।
- **`lowest_gpa`**: `MIN(CASE WHEN stmark.gpa > 0 THEN stmark.gpa ELSE NULL END)` - পাস করা শিক্ষার্থীদের মধ্যে সর্বনিম্ন জিপিএ।
- **`a_plus_count`**: `SUM(CASE WHEN stmark.gpa = 5.00 THEN 1 ELSE 0 END)` - A+ (জিপিএ ৫.০০) প্রাপ্ত শিক্ষার্থীর সংখ্যা।

## ধাপ ৩: ব্যাকএন্ড - জব এবং এপিআই (API) তৈরি

- [x] **জব ইন্টিগ্রেশন:** ব্যবহারকারী যখন একটি নতুন ডেটাসেট বিশ্লেষণের জন্য অনুরোধ করবে, তখন `analytics_jobs` টেবিলে `teacher_performance` ধরনের একটি নতুন জব যুক্ত করার ব্যবস্থা করা।
- [x] **ব্যাকগ্রাউন্ড প্রসেস:** একটি ম্যানুয়াল ট্রিগার তৈরি করা যা `analytics_jobs` টেবিল থেকে `Pending` জবগুলো নিয়ে ধাপ ২-এর স্ক্রিপ্টটি চালাবে এবং জবের স্ট্যাটাস (Running, Completed, Failed) আপডেট করবে।
- [x] **API এন্ডপয়েন্ট তৈরি:** একটি API ফাইল (যেমন: `api/get_teacher_performance.php`) তৈরি করা যা ফ্রন্টএন্ড থেকে পাঠানো অনুরোধ (যেমন: `teacher_id`, `exam_id`) অনুযায়ী `analytics_teacher_performance` টেবিল থেকে ডেটা `JSON` ফরম্যাটে ফেরত দেবে।

## ধাপ ৪: ফ্রন্টএন্ড - ইউজার ইন্টারফেস (UI) তৈরি

- [x] একটি নতুন পেজ তৈরি করা (যেমন: `teacher-performance.php`) যেখানে পারফরম্যান্স রিপোর্ট দেখানো হবে।
- [x] **ফিল্টার UI:** ব্যবহারকারীর জন্য সেশন, পরীক্ষা, শ্রেণি, এবং শিক্ষক অনুযায়ী ডেটা ফিল্টার করার জন্য ড্রপডাউন মেনু তৈরি করা।
- [x] **ডেটা প্রদর্শন:**
    - [x] AJAX ব্যবহার করে ফিল্টার পরিবর্তনের সাথে সাথে ধাপ ৩-এর API কল করে ডেটা আনা।
    - [x] বিষয়ভিত্তিক এবং শ্রেণিভিত্তিক বিস্তারিত পারফরম্যান্স দেখানোর জন্য টেবিল (Table) ব্যবহার করা।
- [x] **ডেটা ভিজ্যুয়ালাইজেশন:**
    - [x] Chart.js লাইব্রেরি ইন্টিগ্রেট করা।
    - [x] বিভিন্ন শিক্ষকের পারফরম্যান্স তুলনার জন্য বার চার্ট (Bar Chart) তৈরি করা।
    - [ ] একজন শিক্ষকের বিভিন্ন পরীক্ষার ফলাফলের অগ্রগতি দেখানোর জন্য লাইন চার্ট (Line Chart) তৈরি করা। (ঐচ্ছিক)

## ধাপ ৫: চূড়ান্তকরণ এবং পরীক্ষা

- [ ] সম্পূর্ণ ফ্লো (UI থেকে ডেটা প্রসেসিং এবং প্রদর্শন পর্যন্ত) পরীক্ষা করা।
- [ ] বিভিন্ন এজ কেস (Edge Case) যেমন - কোনো শিক্ষার্থীর ডেটা না থাকা, রুটিনে শিক্ষক না থাকা ইত্যাদি হ্যান্ডেল করা।
- [x] ব্যবহারকারীর রোল অনুযায়ী পারমিশন চেক করা (যেমন: শুধুমাত্র অ্যাডমিন বা প্রধান শিক্ষক পুরো রিপোর্ট দেখতে পারবেন)।